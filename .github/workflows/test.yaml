name: "CI - Test Features"
on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

jobs:
  test-debian:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        features:
          - rye
          - cypress
    steps:
      - uses: actions/checkout@v4

      - name: "Install latest devcontainer CLI"
        run: npm install -g @devcontainers/cli

      - name: "Generating Debian tests for '${{ matrix.features }}'"
        run: devcontainer features test -f ${{ matrix.features }} -i debian --preserve-test-containers
  test-ubuntu:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        features:
          - rye
          - cypress
    steps:
      - uses: actions/checkout@v4

      - name: "Install latest devcontainer CLI"
        run: npm install -g @devcontainers/cli

      - name: "Generating Debian tests for '${{ matrix.features }}'"
        run: devcontainer features test -f ${{ matrix.features }} -i ubuntu --preserve-test-containers

  test-arch:
    name: "Run tests on Arch Linux"
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - name: "Install latest devcontainer CLI"
        run: npm install -g @devcontainers/cli

      - name: "Test Arch Linux"
        run: devcontainer features test -i archlinux -f rye --preserve-test-containers
